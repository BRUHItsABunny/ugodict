package constants

const (
	baseURL              = "https://api.urbandictionary.com/v0/"
	EndpointDefine       = baseURL + "define"
	EndpointRandom       = baseURL + "random"
	EndpointAutoComplete = baseURL + "autocomplete-extra"
	EndpointVote         = baseURL + "vote"
)
